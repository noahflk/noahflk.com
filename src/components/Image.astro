---
export interface Props {
  src: any;
  alt: string;
}

import { Image as AstroImage } from 'astro:assets';

const { src, alt } = Astro.props;
---

<AstroImage src={src} alt={alt} class="rounded" />
